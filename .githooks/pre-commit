#!/usr/bin/env bash
set -euo pipefail

# rustfmt で整形チェック
(
  cd src-tauri
  cargo fmt --all -- --check
)

# clippy で静的解析（警告をエラー扱い）
(
  cd src-tauri
  cargo clippy --all-targets --all-features -- -D warnings
)
